[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rf-server"
version = "0.3.0"
requires-python = ">=3.10"
description = "RF spectrum streaming server"
dependencies = []
